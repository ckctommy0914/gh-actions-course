name: 01 - Building Blocks

on: 
   workflow_dispatch:
   #push  #whenever we push, execute this workflow

jobs:
    echo-hello:
        runs-on: ubuntu-latest
        steps: 
            - name: Say hello
              run: echo "Hello, world!"

    echo-goodbye:
        runs-on: ubuntu-latest
        steps:
            - name: Successful step  #sth went wrong, the next step will not be executed by default
              run:  |
                 echo "I will success"
                 exit 0
            - name: Say Goodbye
              run: echo "Goodbye!"       
